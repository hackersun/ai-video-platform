# 模型中心恢复验收报告（2026-07-18）

## 结论

`codex/model-center-recovery` 已完成模型中心恢复计划的代码、自动化测试和前端验收。模型连接、目录、能力绑定、组合预设、提示词版本、测试实验室和发布前检查均已形成真实的前后端闭环；旧 `/prompt-skills` 入口继续兼容。

正式数据库保持未迁移状态。本轮只读预检及真实数据库副本迁移均通过；在执行正式迁移前，当前 `sunqy` 页面仍只会显示 8 条已启用提示词，另外 6 条已停用提示词将在正式迁移后恢复到模型中心。

## 交付范围

- 模型中心概览按连接、模型档案、能力绑定、提示词、组合预设和认证结果计算真实阻塞项。
- 模型目录、连接、绑定和组合预设支持服务端分页、可读关联字段、创建、编辑、校验、发布、停用、影响查看及回滚。
- PromptProfileVersion 作为规范提示词版本；PromptSkill 作为兼容投影，创建、编辑、克隆、启停、删除、AI 优化和预览继续可用。
- 提示词工作台恢复正文、版本历史、差异、路由、发布影响、回滚、AI 辅助优化和预览。
- AI 优化明确区分“已配置文本模型”和“仅本地规则（不调用模型）”；本地模式通过 `__local_rules__` 显式传递，后端不会尝试供应商调用。
- 四章工作台实时绑定校验优先使用当前可见配置，并可回退到运行快照中已经持久化的精确配置 ID，避免内部验收配置被产品列表隐藏后误报“缺少模型配置”。
- 测试实验室只展示服务端批准的兼容组合，并返回脱敏认证证据。

## 正式数据库安全证据

用户：`sunqy`（用户 ID `56ae84de-951f-4e74-ac79-3550d6f6f3b2`）。

- 正式库迁移前只读审计：14 条 PromptSkill，14 条正文非空，8 条启用、6 条停用，0 条规范版本关联，0 个正文冲突。
- 迁移预检：计划补建 6 个 PromptProfile、6 个 PromptProfileVersion，并建立 14 条关联；0 个冲突。
- 备份：`backend/output/ai_video-before-prompt-recovery-20260718.db`
  - 权限：`0600`
  - 大小：668,315,648 bytes
  - SHA-256：`c15011cc8959f49cc8910f908a9b5685a791ed724550a497332d86e03d676434`
- 副本：`/tmp/ai-video-model-center-migration-canary-20260718-69f5d2d0-c8b7-4dad-8e60-caa2770fb939.db`
- 副本应用结果：新增 6 个档案、6 个版本，建立 14 条关联，无冲突。
- 副本二次预检：档案、版本和关联待写入数全部为 0，证明迁移幂等。
- 副本迁移后审计：14/14 已关联、14/14 正文哈希一致、0 哈希不一致、0 孤儿关联、0 冲突。
- 临时后端读取副本后，`GET /api/v1/model-center/prompt-profiles?page=1&page_size=50` 返回 14 条，其中 8 条 `published`、6 条 `disabled`。

正式库未执行 `--apply-prompts`，因此没有不可逆的数据写入。

### 合并验证事故与恢复

本地合并后的第一次后端回归误用了主目录默认数据库。`tests/test_model_center_api.py` 当时直接复用应用全局引擎，并在模块夹具退出时执行 `Base.metadata.drop_all()`，导致正式 SQLite 的表结构被清空。

- 事故发现后立即停止后端，并将空库保留为 `backend/output/ai_video-empty-after-test-drop-20260718-2250.db`。
- 正式库已从 19:50 创建并校验的 `ai_video-before-prompt-recovery-20260718.db` 恢复。
- 恢复后 `PRAGMA quick_check=ok`、65 张表、913 条 PromptSkill，`sunqy` 用户存在；正式库 SHA-256 与备份完全一致。
- 对空库取证副本执行 `.recover`，得到的 140 万条记录只存在于无原表结构的 `lost_and_found`，且超长 JSON 存在截断，不能安全回写。
- 备份与事故间约有 2 小时 59 分钟恢复点间隔；当前证据不足以保证该时间窗内不存在业务写入，因此本报告明确保留这项数据风险，不做无依据的“零数据损失”声明。
- 根因已修复：破坏性模型中心 API 测试始终自建 `/tmp` SQLite，引擎、会话、建表和清表均与应用数据库隔离。
- 安全回归：模型中心 API 38/38 通过；正式库测试前后 SHA-256 完全一致；完整模型中心隔离回归 187 项通过。

## 自动化验证

### 模型中心后端

执行计划中 11 组恢复与兼容测试：`186 passed, 1 warning`。唯一警告是测试环境未设置持久化 `FERNET_KEY`，使用瞬时密钥；不影响断言结果，但生产环境仍必须配置稳定 Fernet 密钥。

覆盖：提示词恢复和幂等、版本不可变性、旧 PromptSkill 路由、AI 辅助入口、模型中心 API/Repository、绑定解析、组合预设合同、回填与影子比较。

### 前端

- `npm run typecheck`：通过。
- `NEXT_DIST_DIR=.next-model-center-recovery-verification npm run build`：通过，45 个路由完成生产构建。
- 模型中心 Playwright：27/27 通过，包含 API 脱敏边界、分页筛选、连接/档案/绑定/组合预设维护、提示词正文与版本、显式本地优化、测试实验室和旧入口兼容。

### 四章前端确定性验收

标准命令：`npm run verify:four-chapter`（仓库根目录）。

- 后端聚焦回归：86/86 通过。
- Chromium 四章工作台：5/5 通过，用时约 1 分钟。
- 两个关键镜头：2 个确定性媒体任务，12 项一致性评估。
- 完整六镜头：6 个确定性媒体任务，36 项一致性评估。
- 模糊必需实体：前端显示阻塞，Story Lock 0 写入。
- SQLite 血缘审计：通过。
- 开发数据库和 `frontend/tsconfig.json` 执行前后校验一致。
- 脱敏日志：`/tmp/ai-video-platform-four-chapter-output-a26e5ec5-d49c-41b6-9d38-36989f915f09/acceptance.log`
- 日志 SHA-256：`46503d9617bb192210201b97af73264b2556798844be8035d7b2c5e5c4c35779`

本验收没有调用付费图像、语音或视频模型。真实页面检查期间曾因旧下拉语义不明确尝试一次文本优化请求，供应商返回 HTTP 400 且没有返回可用优化结果；随后已改为显式本地模式并补充前后端回归测试。不能仅凭本地证据判断供应商是否对该失败请求计费。

## 真实 `sunqy` 前端验收

真实数据库保持只读，通过功能分支前端和后端检查：

- 概览：展示 8 个真实阻塞项，不再误报“没有阻塞项”。
- 连接：14 条；目录：166 条、9 页；绑定：4 条；组合预设：5 条。
- 测试实验室：199 个服务端认证候选，当前认证历史 0 条。
- 提示词：正式库迁移前显示 8 条已启用档案，符合当前数据状态；副本 API 验证迁移后为完整 14 条。
- “返回工作台”保留 `workflow_id` 上下文并成功返回 `/studio`；返回后的浏览器控制台 0 error、0 warning。

截图目录：`output/playwright/model-center-recovery-20260718/`

- `01-prompts-real-sunqy.png`
- `02-prompts-local-optimization.png`
- `03-overview.png`
- `04-connections.png`
- `05-catalog-page-1.png`
- `06-catalog-page-2.png`
- `07-bindings.png`
- `08-recipes.png`
- `09-test-lab.png`
- `10-return-studio.png`

## 上线前唯一待决事项

代码分支可以进入合并流程。要让正式 `sunqy` 页面从 8 条提示词恢复到完整 14 条，还需在合并并重启服务后，使用已核验备份执行一次正式 `--apply-prompts`，随后立即运行 `--check-prompts` 和只读哈希审计。该操作是本计划保留的显式数据变更决策点，本报告未代替用户授权执行。
